const initialState = {
  shop: [],
};

export const shopReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};



export const fetchShopTC = ()=>{
    return (dispatch)=>{
        
    }
}